# 2026-09-18

## 全面技术审查（技术债 + 潜在 bug）

产出：`docs/technical-debt-audit-2026-09-18.md`

**仓库基线（本次实测）**
- `tsc --noEmit`：0 错误
- `vitest run`：72 文件 / 247 用例通过，9 跳过
- `eslint .`：0 错误 / 234 警告（全是未使用变量，集中在 `src/shengtian-reference/**`）
- 无 SQL 注入；无 `@ts-ignore`；`as any` 8 处全在死代码；无 TODO/FIXME
- 331 个 TS/TSX，41,604 行（活代码 23,904，`shengtian-reference` 17,692）

**确认的 P1（7 项）**
1. `api/catalog/{deep-archives,world-pulse,personas}/route.ts` 零鉴权，`deep-archives` 直接返回 `isUnlocked:false` 条目的完整 `finalRippleSequence`（`ecosystem.ts:57-70`），而 UI 在 `DeepArchivesModal.tsx:94` 是要扣 `deep_archive_unlock` 的 → 付费内容白送
2. `api/agent/route.ts:268-277` 先出结果后 commit，commit 抛错则 catch 走 release → 免费拿全文
3. `lib/agent/chat.ts:650` 无 AbortSignal；`chat.ts:762` / `bazi-personality.ts:69` 无 `max_tokens`
4. `api/agent/route.ts:20-21` payload 上限 440KB 无截断
5. `lib/db/pool.ts` 无 `pool.on("error")` → 空闲连接报错会终止 Node 进程
6. `ops/migrate.mjs:19` 无 advisory lock，读一次 applied → 并发部署竞态
7. 死代码 48 文件 / 15,634 行（`App.tsx` 整棵树不可达；`ShengtianReferenceEntry` 无引用）

**重点 P2**
- 幂等键投毒：`extended-repository.ts:141` 未传 header 时把客户端 `diagnosis._idempotencyKey` 原样落库，撞 `011_review_idempotency.sql:7-8` 唯一索引
- `battle_reviews` 缺 `battle_id` 索引；记忆查询按 `source_json->>'recordId'` 无索引无唯一约束 → 并发重复
- 平台边界：访客分支跳过 gate（`agent/route.ts:209-211`）；AI 重放不重跑 gate（`handler.ts:51-65`）；`bazi-personality` 仅 HMAC 无 gate
- 时区/日界不一致：`qimen/chart.ts:42-47` 忽略 `input.timeZone`；`ziwei/chart.ts:30-40` 与 `bazi/chart.ts:248` 日界策略不同（23:00 归属可能冲突）；`qimen/sequence.ts:130-137` `setUTCMonth` 月份溢出
- `api/[...path]/route.ts:22-28` 模块级快照跨用户共享

**已知的正面基线（后续别改坏）**
- gate 在服务端强制（`agent/route.ts:192-208`），全链路 fail-closed，未发现 fail-open
- 身份一律取 `gate.subject_id`（`account-subject.ts:11-19`）
- 无本地 OTP / 公司用户表 / 订单 / 会员真相表
- 全仓库无 `dangerouslySetInnerHTML`

**项目约定补充**
- 迁移在 `database/migrations/`（不是 `database/*.sql`），有 checksum 账本
- 路由重复度高：72 个 route.ts，51 个用 `requireAccountSubject`，26 个共享同一 GET 骨架；应参照 `handleAiPost` 的委托模式收敛
- 同时存在 `package-lock.json` 与 `pnpm-lock.yaml` → 包管理器不唯一
- `.env.example` 主机名（`consumer-api.singularitysequence.com`）与 `.env.local`/README/AGENTS.md（`api.singseq.com`）不一致

---

## P1 全部修复完成（同日）

用户指示：「P1全部修复」+「public/gods-eye-view/ 不要」。

1. **P1-1 付费内容泄露** — `api/catalog/{deep-archives,world-pulse,personas}` 加 `requireAccountSubject`；deep-archives 新增 `withheldUnlessUnlocked`，未解锁条目剥离 `finalRippleSequence`。新增 `extended-repository.listUnlockedArchiveIds(subject)` 汇总账号级已解锁档案（折叠各战局最新 `deep-archives` 模块状态）
2. **P1-2 计费漏洞** — `api/agent/route.ts` 新增 `delivered` 标志：模型产出后不再释放预留；commit 改 3 次重试；流式路径用 `streamAgentAnalysis` 新增的 `onChunk` 标记交付，`onAbort` 不再退款
   - **`battles/[id]/ai/[kind]/handler.ts` 原本就安全**（`commitAttempted` 在 commit 前已置位），审计报告该条为误报
3. **P1-3 LLM 无超时** — `chat.ts` 导出 `AGENT_REQUEST_TIMEOUT_MS=90s`、`BAZI_PERSONALITY_MAX_TOKENS=4000`；`requestAgentAnalysis` 加 signal；两处重复的 `requestBaziPersonalityPrediction`（chat.ts + bazi-personality.ts）加 signal + max_tokens。`platform/server.ts` 新增 `fetchPlatform()` 统一 10s 超时并转 504 `platform_request_timeout`
4. **P1-4 payload 无上限** — 实测最大合法载荷：三盘联合 structuredText 14,946 / jsonPayload 23,980 字符（**序列从不提交，只发 active chart**）。上限 180k/260k → 60k/80k，并新增总量 120k 校验
5. **P1-5 连接池崩溃** — `pool.ts` 加 `pool.on("error")`、`statement_timeout` 默认 30s、`DATABASE_POOL_MAX` 校验、ROLLBACK 不再覆盖原始错误、污染连接 `release(true)` 丢弃
6. **P1-6 迁移竞态** — `ops/migrate.mjs` 用 `pg_advisory_lock(8274513096)` 包住整个 apply，锁内重读账本（同客户端执行，因 pool `max:1` 避免自锁）
7. **P1-7 死代码 + gods-eye** — 删除 `src/shengtian-reference/`（53 文件）、`public/gods-eye-view/`（421 文件 / 31MB）、`src/app/gods-eye-view/page.tsx`、`src/components/shengtian-reference-entry.tsx`、`world-pulse-static-release.test.ts`、globals.css 中 `.gods-eye-*`
   - 保留并迁移：`PlatformCallbackView` → `src/components/platform-callback-view.tsx`；`BillingResultView` → `src/components/billing-result-view.tsx`；`index.css` → `src/app/platform-views.css`；`AI_PERSONA_CONFIGS` → `src/lib/catalog/personas.ts`；`WorldPulseEvent`/`DeepArchiveItem` 内联进 `ecosystem.ts`
   - `package.json` css:audit 路径同步更新；顺带清空 13 个 `.next*` 陈旧构建缓存

**验证**：tsc 0 错误；vitest 70 文件 / 242 通过（−5 为随死代码删除的测试）；`npm run build` 成功；eslint **234 警告 → 3 警告**
**规模变化**：src TS/TSX 41,604 → 24,255 行；public 31MB → 11K；git diff +747 −49,216

### ⚠️ 事故记录（务必避免重演）

执行 `git rm -r -q --ignore-unmatch src/shengtian-reference public/gods-eye-view` 后，**整个 `src/` 与 `public/` 从工作区消失**（340 个文件）。根因未确证，但与该 `git rm -r` 调用同时发生。

- 恢复：`git reset -q && git checkout -- .` 从 HEAD 还原全部跟踪文件
- 未提交的 src/ 编辑全部丢失，后依据 `~/.workbuddy-ai/changes-detail/<conversationId>/*.json` 的 hunks 与对话上下文逐条重放
- **结论：本仓库禁止用 `git rm -r` 删目录。改用 `rm -rf <显式路径>`，每个路径单独执行并验证后再继续；删除前先 `git add -A` 建立索引级恢复点。**（实测 `rm -rf` 行为正常，无连带删除）


import { NextResponse } from "next/server";
import { AccountSubjectError, requireAccountSubject } from "@/lib/agent/account-subject";
import { asRecord } from "@/lib/battle/input";
import { detectAllocationConflicts, getStrategyProfile, listCalibration, listPlaybook, saveStrategyProfile } from "@/lib/battle/extended-repository";
export async function GET(request:Request){try{const subject=await requireAccountSubject(request);const [profile,calibration,conflicts,playbook]=await Promise.all([getStrategyProfile(subject),listCalibration(subject),detectAllocationConflicts(subject),listPlaybook(subject)]);const byDimension=Object.groupBy(calibration,(item)=>item.dimension);const calibrationSummary=Object.fromEntries(Object.entries(byDimension).map(([key,items])=>[key,{count:items.length,meanAbsoluteError:items.length?items.reduce((sum,item)=>sum+Math.abs(item.error??0),0)/items.length:null}]));return NextResponse.json({profile,calibrationSummary,allocationConflicts:conflicts,privatePlaybook:playbook});}catch(error){return error instanceof AccountSubjectError?NextResponse.json({error:error.message},{status:error.status}):NextResponse.json({error:"读取战略档案失败。"},{status:500});}}
export async function PUT(request:Request){try{const body=await request.json().catch(()=>null) as {profile?:unknown}|null;const profile=asRecord(body?.profile);if(!profile)return NextResponse.json({error:"战略档案无效。"},{status:400});return NextResponse.json({profile:await saveStrategyProfile(await requireAccountSubject(request),profile)});}catch(error){return error instanceof AccountSubjectError?NextResponse.json({error:error.message},{status:error.status}):NextResponse.json({error:"保存战略档案失败。"},{status:500});}}

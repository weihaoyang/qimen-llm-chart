"use client";

import ReferenceApp from "@/shengtian-reference/App";

/** The product root is the frontend shipped in the reference archive. */
export function ShengtianReferenceEntry() {
  return <ReferenceApp />;
}
